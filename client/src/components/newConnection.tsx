'use client'

export default function newConnection(props: any) {
  return (
    <div>
      <h1>New Connection Component</h1>
      <button onClick={() => console.log('hello')}>Hello</button>
    </div>
  )
}
